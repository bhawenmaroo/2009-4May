/* Scene removed for the light theme. The site's visual identity now
   comes from photography, the CSS-3D molecules, tilt cards and parallax —
   no fullscreen WebGL canvas underneath. */
export function Scene() {
  return null;
}
